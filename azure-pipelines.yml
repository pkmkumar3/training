# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main
stages:
  - stage: Build_stage
    displayName: Build
    jobs:
      - job: Build
        displayName: Build
        pool:
          name: mypool
        steps:
      
        - task: Docker@2
          inputs:
            containerRegistry: 'docker_training'
            repository: 'pkmkumar3/training'
            command: 'build'
            Dockerfile: '**/maven'
        - task: Docker@2
          inputs:
            containerRegistry: 'acr'
            repository: 'lab141'
            command: 'push'